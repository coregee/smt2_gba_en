# English reference material

`source/` contains saved external reference pages and translation passes. `generated/` contains
normalized JSON produced from those snapshots plus the generated combatant reference table.
`script/` owns the parsers that connect the two.
These files are reference inputs, not the editable game corpus.
